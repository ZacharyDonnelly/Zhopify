import '@/lib/utils/env';
import Banner from '@/ui/components/banner';

const Home: React.FC = () => {
  return (
    <section className='w-full pt-1'>
      <Banner />
    </section>
  );
};

export default Home;
